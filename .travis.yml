notifications:
  email:
    recipients:
      - onurrozkaan@gmail.com

    # change is when the repo status goes from pass to fail or vice versa
    on_success: change
    on_failure: always

language: node_js

branches:
  # whitelist
  only:
    - master
  # blacklist
  except:
    - test
